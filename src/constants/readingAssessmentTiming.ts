/** Student-facing practice test duration (timer and copy), regardless of legacy DB values. */
export const PRACTICE_TEST_MINUTES = 20;

/** Full group / final reading test — total session time. */
export const GROUP_TEST_TOTAL_MINUTES = 60;

/** Suggested time budget per passage (shown alongside total); resets when changing passage. */
export const GROUP_TEST_PASSAGE_MINUTES = 20;
