import { redirect } from "next/navigation";

/** Integrated lessons retired — use Level Builder (practice + final tests). */
export default function IntegratedLessonsPage() {
  redirect("/dashboard/instructor/reading-levels");
}
